---
"playhtml": patch
---

Update playhtml's default PartyKit hosts to the Cloudflare WAF-protected custom API domains so production pages connect through `api.playhtml.fun` and staging pages connect through `api-staging.playhtml.fun`.
