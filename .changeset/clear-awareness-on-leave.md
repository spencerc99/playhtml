---
"playhtml": patch
"@playhtml/react": patch
---

Clear element awareness snapshots when a peer leaves so callbacks and views stop seeing stale ephemeral user state. Keep the React package test double aligned with the configured initialization path.
