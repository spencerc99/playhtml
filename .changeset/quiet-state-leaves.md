---
"@playhtml/common": patch
"playhtml": patch
---

Add shared helpers for editing existing primitive state leaves and use them in the playhtml development toolbar so developers can update string, number, boolean, and null values inline without replacing whole JSON blobs.
