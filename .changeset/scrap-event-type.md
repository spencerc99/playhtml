---
"@playhtml/extension-types": minor
---

Add `scrap` to `CollectionEventType` and `getValidEventTypes()`. Scrap events record distinctive images seen while browsing (image URL plus page provenance) for the internet-scraps collage. They are collected locally by the extension and are not uploaded to the worker.
