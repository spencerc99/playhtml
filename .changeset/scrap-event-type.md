---
"@playhtml/extension-types": minor
---

Add `image` to `CollectionEventType` and `getValidEventTypes()`. Image events record distinctive images seen while browsing (image URL plus page provenance) for the extension's internet-scraps collage. They are collected locally by the extension and are not uploaded to the worker.
